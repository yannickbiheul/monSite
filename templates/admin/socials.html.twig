{% extends 'base.html.twig' %}

{% block title %}Administration Réseaux Sociaux{% endblock %}

{% block body %}

    <div class="container-fluid" style="min-height: 90vh;">
        <h1 class="my-5 text-center">
            Mes réseaux sociaux
        </h1>
        <div class="text-center md-3">
            <a href="{{ path('admin_socials_new') }}" class="btn btn-success text-white mb-3">
                <i class="fas fa-plus"></i> 
                Ajouter un réseau
            </a>
        </div>
        <table class="table table-hover my-5">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>URL</th>
                    <th>Logo</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for social in socials %}
                    <tr>
                        <td>{{ social.id }}</td>
                        <td>{{ social.titre }}</td>
                        <td>{{ social.url }}</td>
                        <td>{{ social.logo | raw }}</td>
                        <td>
                            <a href="{{ path('admin_socials_edit', {'id': social.id}) }}" class="btn btn-primary">
                                <i class="fas fa-edit"></i> 
                                Éditer
                            </a>
                            <a href="{{ path('admin_socials_delete', {'id': social.id}) }}" class="btn btn-danger">
                                <i class="fas fa-trash"></i> 
                                Supprimer
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}